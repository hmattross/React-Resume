import React from 'react';
import { Panel } from 'react-bootstrap';

class Languages extends React.Component {
    render() {
        return(
            <Panel header="Languages">
                React, Java, JavaScript, HTML/CSS, JQuery, C/C++/C#, MYSQL, Verilog
            </Panel>
        );
    }
}

export default Languages;